// METRIC numeric core — curated public facade for the graph-primitive contracts.
//
// Small METRIC-owned entry point for the promoted mtrc::numeric raw graph
// primitives (index-pair generation and validation, edge selection/grouping,
// symmetrization, degree/connectivity/stretch accumulators, and all-pairs
// shortest paths). These are domain-neutral numeric tools over integer record
// indices and edge weights; they are NOT finite-metric-space graphs. The
// contracts are first-party METRIC C++ (see metric/numeric/README.md for
// provenance and licensing). This facade declares no symbols of its own; it only
// re-exports promoted core headers.

#ifndef METRIC_NUMERIC_GRAPH_HPP
#define METRIC_NUMERIC_GRAPH_HPP

#include <metric/numeric/math/GraphPrimitives.h>

#endif
