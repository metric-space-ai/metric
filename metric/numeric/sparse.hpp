// METRIC numeric core — curated public facade for the sparse container contracts.
//
// Small METRIC-owned entry point that groups the promoted mtrc::numeric sparse
// containers (compressed matrix and compressed vector) so callers can include
// exactly the surface they need. The contracts are first-party METRIC C++ (see
// metric/numeric/README.md for provenance and licensing). This facade declares no
// symbols of its own; it only re-exports promoted core headers.

#ifndef METRIC_NUMERIC_SPARSE_HPP
#define METRIC_NUMERIC_SPARSE_HPP

#include <metric/numeric/math/CompressedMatrix.h>
#include <metric/numeric/math/CompressedVector.h>

#endif
